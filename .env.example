# Browser-visible URL of the data service. Do not include a trailing slash.
VITE_API_URL=http://localhost:8000

# Filesystem path used by the Python API process.
PSX_DB_PATH=/path/to/psx_watcher.db

# Read-only root containing the accepted artifacts/{models,predictions}/c7 files.
PSX_ML_C7_ROOT=/home/hassan/psx-ml-research
